node_modules

.DS_Store
.DS_Store?
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

/vendor/
/.playwright/
/coverage/
/.phpunit.result.cache

# Local runtime-config override (see fixtures/README.md)
fixtures/config-local.php

# Local release-ZIP dry runs (see docs/vip-integration.md).
/dist/
/*.zip

# Built JavaScript assets: releases build these in CI (see release.yml), and
# development checkouts build them with `npm run build`.
/build/
